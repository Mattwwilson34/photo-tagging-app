import React from 'react';
import { render } from '@testing-library/react';
import CharacterKey from '../CharacterKey';

describe('CharacterKey component', () => {
  it('Does something...', () => {

  });
});
