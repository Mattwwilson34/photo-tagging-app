import React from 'react';
import { render } from '@testing-library/react';
import TargetArea from '../TargetArea';

describe('TargetArea component', () => {
  it('Does something...', () => {

  });
});
